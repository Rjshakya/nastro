/***
 *
 *
 *  nastro-cms init
 *   - search for pages that users has gave its access 
 *   - ask user to select page/db from all these pages.
 *   -  create a file nastro.ts
 *     - it has fetch handler , that will fetch content from nastro
 *     - it will also gen nastro-types.d.ts - it will have type about result from the fetch handler
 *     - type will be decided by the notion - db id based on the db content it will generate type for it.
 * 
 *     - and make or duplicate opinionated nation - cms ;
 *     - with callout blocks
 * 
 *    
 *  - flow 
 *    - user will authenticate to nastro using notion - oauth
 *    - nastro will get access to user's accessToken
 *    - they two option either call nastro - cms init ;
 *    - follow the steps manually ; installing sdk and setting it up .
 *    
 * 
 *
 *
 *
 *
 */

















